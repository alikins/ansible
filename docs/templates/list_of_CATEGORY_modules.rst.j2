@{ title }@ @{ plugin_type }@
@{ '`' * title | length }@````````

{% if blurb %}
@{ blurb }@

{% endif %}


{# split list_of_all into it's own template? #}
{% if category_label == 'all' %}

{% if category['_modules'] %}
{% for module in category['_modules'] | sort %}
    * :doc:`@{ module }@{% if module_info[module]['deprecated'] %} **(D)**{% endif%} - @{ module_info[module]['doc']['short_description'] }@ <@{ module }@_module>`
{% endfor %}

{% endif %}
{% endif %}

{% if subcategories  and category_label != 'all'%}
{% for name, info in subcategories.items() | sort %}
@{ name.title() }@
@{ '-' * name | length }@

.. toctree:: :maxdepth: 1

{% for module in info['_modules'] | sort %}
    @{ module }@_module
{% endfor %}

{% endfor %}
{% endif %}

.. note::
    - **(D)**: This marks a module as deprecated, which means a module is kept for backwards compatibility but usage is discouraged.
      The module documentation details page may explain more about this rationale.

