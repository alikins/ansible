- hosts: testhost
  # become: yes
  gather_facts: false
  vars_files:
    - vars/mgmt.yml
    - vars/mgmt_vault.yml

     #  roles:
      #    - external/geerlingguy.mysql
      #    - external/geerlingguy.gogs
      #
  tasks:
    - name: show vault_mysql_root_password var
      debug:
        var: vault_mysql_root_password

    - name: show vault_gogs_db_password password var
      debug:
        var: vault_gogs_db_password

    - name: show vault_some_other_password_encrypted_with_hunter42 var
      debug:
        var: vault_some_other_password_encrypted_with_hunter42
